# Optional local server settings.
HOST=127.0.0.1
PORT=3000

# Optional M6 demo only. Set in the process environment; never commit a real PAT.
# Public read-only PAT, with expiration and no write/private-repository access.
GITHUB_PERSONAL_ACCESS_TOKEN=replace_with_public_read_only_token
REAL_MCP_TIMEOUT_MS=5000
